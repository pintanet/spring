spring:
  # 1. Configurazione H2 in-memory per il database
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  jpa:
    hibernate:
      # Scegli 'none' o 'validate' se non hai entità JPA, per evitare errori.
      ddl-auto: none